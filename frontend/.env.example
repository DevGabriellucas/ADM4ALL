# URL base da API do backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Use apenas em desenvolvimento para simular o login sem chamar o backend.
# Perfis aceitos: aluno, instrutor, coordenador.
NEXT_PUBLIC_USE_AUTH_MOCK=false
NEXT_PUBLIC_AUTH_MOCK_PROFILE=aluno

# Opcional: ID do instrutor exibido enquanto nao ha login/sessao.
# Padrao = instrutor de teste do seed (Eduardo Lima).
INSTRUTOR_ID=20000000-0000-0000-0000-000000000001
