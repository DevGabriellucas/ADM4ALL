# URL publica da API do backend no deploy.
NEXT_PUBLIC_API_URL=https://api.seu-dominio.edu.br

# URL usada apenas pelo servidor Next.js. Em Docker Compose local, use:
# API_INTERNAL_URL=http://backend:8000
API_INTERNAL_URL=https://api.seu-dominio.edu.br
